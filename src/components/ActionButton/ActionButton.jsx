import React from 'react';
import Button from 'react-bootstrap/Button';

const ActionButton = () => {
    return (
        <Button variant='primary' size='sm' className='my-1'>coming later</Button>
    );
}

export default ActionButton;